/**
 * Shared Types Library
 *
 * This library contains shared TypeScript types and interfaces
 * used by both the API (NestJS) and Web (React/Vite) applications.
 */

// Re-export types from subdirectories
export * from './types';
